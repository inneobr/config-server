spring:
  application:
    name: server-config
  cloud:
    discovery:
     enabled: true
    gateway:
      discovery:
        locator:
          enabled: true 
          lowerCaseServiceId: true
    config:
      server:
        git:
          uri: https://github.com/inneobr/config-repo
          search-paths:
            - dev
            - prod    

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:7100/eureka
  instance:
    instanceId: ${spring.cloud.client.ip-address}:${server.port}
            
server:
  port: 7000    
   
logging:
  level:
    root: FATAL